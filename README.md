# Mimi 语音教练

产品界面使用英文，角色与显示名称统一为 Mimi。保留教案的近白、朱红、蓝灰与细金线，使用用户提供的 Mimi 人物形象。首页突出 Start talking，通话控制固定在底部；History 从左侧打开，Settings 从右侧打开。

交互参考：[ChatGPT Voice](https://help.openai.com/en/articles/20001274/) 的开始、静音、结束与同一对话中的文字输入；[Duolingo Video Call](https://blog.duolingo.com/video-call/) 的角色中心布局；[Duolingo 界面整理](https://blog.duolingo.com/core-tabs-redesign/) 的字体、间距与层级一致性；[ELSA AI](https://elsaspeak.com/en/ai/) 的交流与后续反馈分阶段呈现。这些单人产品的交互案例不能证明三人声纹识别效果。

本工作区持续完善的项目。帮助学生用自己的英语表达、修正和整理观点，支持 **单人单机** 和 **三人共用一台电脑**。两种方式使用同一套对话界面与记录规则。

## 使用方式

访问主应用根路径 `/`。原 `/prototype` 入口现在跳转到真实应用，不再显示示例姓名和模拟对话。

- **模式标签**：顶部保留“Solo｜Group of 3”。结束语音后可切换，优先回到该模式已有的对话；通话中保留当前标签并暂停切换。
- **单人**：点击“Start talking”即可使用，无需填写姓名或登记声音。称呼可选。默认直接与 Mimi 问答。
- **麦克风**：优先使用浏览器的系统默认输入，通话底部用真实输入音量显示收音状态。设备选择收在右上角“Settings”；单人可在通话中切换，小组需结束语音后更换，继续时会检查声音登记与设备是否匹配。
- **三人小组**：依次点击录音，说出称呼并用英语介绍自己约 20 秒；称呼可自动提取，也可直接输入或修改。声音登记成功才保存声纹 ID，失败可单独重录；三人准备后自动转入讨论。
- **回应控制**：小组讨论默认只记录。说“Mimi, …”或点击“Ask Mimi”提出一个问题，触发一次回答后回到记录状态。文字提问也会消耗这次提问状态；后续普通讨论不会继承它。“先听我说”或“just listen”暂停回答，麦克风仍记录。关闭麦克风则停止收音。
- **成员管理**：结束语音后，在“Settings”中修改称呼、重新登记声音或换人。换人使用新的身份，原成员的发言和提纲保留。通话中的名字仅显示归属。
- **发言与提纲**：完整发言直接显示在同一段对话中。结束语音后可按人筛选、修正说话人和转写；个人提纲可编辑、确认和导出，有发言或提纲编辑的原成员仍可选择。更正发言后，受影响的既有提纲标为待核对。现有提纲生成沿用语音服务，入口明确标为“Start call & create outline”，连接状态及结束控制仍显示在主界面。
- **文字输入**：键盘按钮展开当前对话中的输入框。三人文字发言默认“待确认”，不采用上一位说话人的身份；每次发送后重置这个选择。未发送文字和提纲编辑按会话及成员分别保存，关闭面板或刷新后可以继续。
- **继续对话**：同一电脑、同一浏览器、同一站点会保留记录。结束只停止语音，仍留在原对话；重新连接时将现有记录送回教练作为上下文。左上角“History”可切换历史对话、新建对话和导出记录；右上角“Settings”管理成员、麦克风及后台入口。

没有把模型推测当成学生已确认的意思；没有以强制的四轮确认代替自然对话。课堂的文化交流四点框架保留在教练提示词中，按谈话内容使用。

## 实现与维护入口

主应用：[hermes-voice-coach](./hermes-voice-coach/README.md)，React / Vinext；独立版：[hermes-volc-standalone](./hermes-volc-standalone/)，Node / Cloudflare Worker。它们属于同一项目。内部目录和环境变量保留原标识，产品名称为 Mimi。

本目录是 Mimi 的独立 Git 根目录。源码仓库为 [Cr-GH0/4f7e58cb4b3dac247de1b8e6e68c1e94](https://github.com/Cr-GH0/4f7e58cb4b3dac247de1b8e6e68c1e94)，后续提交和推送在本目录执行。上层教学工作区的历史没有导入此仓库。

两端共同使用独立版 `public` 内的模块：

| 文件 | 作用 |
| --- | --- |
| `coach-view.js`、`coach.css` | 对话、成员、记录和提纲界面 |
| `input-drafts.js` | 未发送文字和编辑草稿，按会话及成员分别保存在本机 |
| `voice-runtime.js` | 麦克风、RTC 会话生命周期、登记、回应控制、同步更正 |
| `conversation.js` | 持久身份、多个对话、旧数据迁移、提纲状态 |
| `group-session.js` | 语音归属、字幕合并、录音格式校验 |
| `voice-enrollment.js` | 同一麦克风录制 20 秒 WAV，不在浏览器保存原始录音 |
| `voice-chat-config.js` | 三种服务配置：单人、声音登记、三人识别；统一的教练提示词与更新请求 |

服务端接口为 `/api/session`、`/api/voiceprint/register`、`/api/voicechat/start`、`/api/voicechat/update`、`/api/voicechat/stop`。ASR 使用自动判停，独立输出完整发言。客户端默认关闭远端播放，先取消原生自动回答，再按回应控制决定是否请求新回答；只有更新上下文后明确请求的新轮次才显示、播放。更新失败不会放行原生回答。声音登记期间关闭 LLM。

## 数据与能力边界

- 当前存储范围是本机浏览器，不是跨设备账号。浏览器只保存发言、提纲、称呼和声纹 ID。服务端凭据留在服务器。
- 原 `hermes.group.v1` 数据会迁移为具有持久人员身份的多个对话结构；旧存储保留。加载失败时不覆盖原数据，并提供导出。
- 声音样本会发送至火山引擎。登记需 WAV、16 kHz、16 bit、单声道、15～30 秒，总有效语音不少于 12 秒。录满 20 秒不代表声音质量合格。注册声纹保留在火山端；此轮未增加云端声纹删除功能。
- 三人使用预注册声纹 `VoicePrint.Mode=2`。匹配门限暂为 50，来自官方推荐的 40～60 区间，仍需真实课堂校准。没有匹配、分数不足或检测到混合身份的发言留作“待确认”；不继承上一人的身份。重叠语音的完整分离不在当前能力保证内。
- “叫 Mimi”“先听我说”的判断由明确短语和当前交谈人驱动，并非能够理解所有多人交谈意图的模型。界面提供相同操作，供语音未正确触发时使用。
- 每次回复使用最新更正记录，取最近约 24,000 字符及当前成员提纲；较早内容超出时在传给模型的数据中标明省略。使用方舟置顶的一组中性对话占满 1 轮自动历史，实际历史由应用记录供给，避免错误归属继续留在自动历史中。
- 语音连接到期时结束本次连接并保留记录，可继续连接。用户结束、取消、离开页面时执行清理与停止请求；收到服务端停止确认前不会声称服务端已结束。

## 验证记录（2026-09-05）

本轮自动检查覆盖单人跳过登记、三人登记及重试、同轮连续发言、未知与混合身份、字幕重复与乱序、取消启动后的服务停止、改名与换人保留旧归属、旧数据迁移、恢复模型上下文、人工更正锁定及提纲失效、更新失败不触发回答、页面服务端渲染。

真实本地接口调用已通过：获取会话、单人启动、替换上下文、打断、结束。接口成功代表请求被服务接受，不等于听说链路已完成。

故障一：设备列表的第一个物理输入是 Steam Streaming Microphone 虚拟麦克风，系统默认输入是 POROSVOC。首次修复又错误地依赖 `groupId` 匹配；本机所有输入设备实际返回相同的 `groupId`，因此显示的默认设备名称与实际音轨仍不一致。现在先打开默认别名，从真实音轨识别物理设备，再明确绑定并核对音轨的 `deviceId`。Edge 已实际采集 POROSVOC，并转写真人发言“OK, can you understand what I'm talking about?”。

故障二：重构把 ASR 改为手动判停，又等待 `paragraph=true` 才请求回答，造成互相等待。独立测试通过 RTC 外部音轨输入合成英语语音，复现了文字已返回、结束标记始终为 false 的故障。恢复自动判停后，完整转写、应用回应请求、Mimi 回答和服务端 `answerFinish` 均已收到；测试回答以“Hello! I can hear you clearly”开头。“Just listen. My topic is tea and cultural exchange.”测试保留了两条完整发言，未发出应用回复请求，播放音量始终为零。合成语音测试使用独立内存记录，没有写入用户的本机对话。

语音链路修复后的构建、代码检查及 32 项自动测试通过，用户随后确认单人通话可用。当前使用 Edge；内置浏览器的 RTC 握手曾超时。三人真人声纹登记及教室噪声场景仍待验证。当前回应控制会取消服务端刚启动的原生模型回答，再请求带最新应用上下文的回答；由此产生的额外时延和模型开销尚未量化。不能据这些检查宣称课堂准确率或自然对话效果已达标。代码为本地变更，未发布。

2026-09-05 界面收减后，构建、lint、TypeScript 检查及 37 项自动测试通过。新增检查覆盖输入草稿恢复、换人后旧提纲草稿可访问、小组文字提问消耗提问状态。Edge 页面操作确认原有 31 条记录保留，模式标签切换正常，文字草稿在切面板和刷新后恢复；测试文字未发送，已清除。

同日新增后台后，构建、lint、TypeScript 检查及 41 项自动测试通过。后台新增检查覆盖登录鉴权、无效配置和并发覆盖的拒绝、三类模型配置透传、通话期间固定配置版本、声纹阈值同步以及独立版文件存储恢复。Codex 预览已实际验证入口、密码登录、保存与刷新回读；当前仍使用原模型配置。真实服务接口已读取后台第 1 版配置，启动、更新上下文、结束均返回成功。本轮未重新验证真人完整通话或三人声纹效果。

同日改进后台与菜单后，构建、lint、TypeScript 检查及 44 项自动测试通过。真实账号目录读取到 23 个对话模型版本和 548 种匹配语音合成资源的声音；模型是否开通按接口返回单独标注。Codex 预览已检查模型搜索、声音试听、合成模型与声音匹配、指令范例预览和编辑、撤销、保存及刷新回读。“History”与“Settings”改为文字入口，新建和导出归入记录，成员、麦克风和后台归入设置。保存验证生成配置第 2 版，回读确认配置内容仍与原设置一致，未留下测试指令。本轮未重新测试真人通话或三人声纹效果。

最新界面包含 Listening、Thinking 和 Speaking 状态动效，以及自动增高的文字输入框、桌面 Enter 发送、Shift+Enter 换行和手机键盘适配。首次会话的草稿刷新恢复问题已修复。构建、lint 和 45 项自动测试通过；动效切换、静音及断线使用隔离的模拟通话验证，三人真人声纹效果仍待测试。

## 运行

后台入口位于右上角“Settings”内，也可直接访问 `/admin`。可修改实际使用的对话/提纲大模型、语音识别模型、语音合成模型与音色、声纹匹配阈值，以及对话与提纲提示词。对话模型和声音自动读取官方目录，可搜索名称、试听声音；教练指令按用途编辑，提供使用说明和中文范例。声纹使用固定接口，目前没有独立的型号切换。保存后对新语音连接生效，已有连接继续使用原配置。登录密码由服务端 `HERMES_ADMIN_PASSWORD` 设置。

主应用首次启用后台前，运行 `npm run db:migrate` 创建本地 D1 配置表；本次工作区已经执行。主应用配置存放在 `.wrangler/state`，独立 Node 版存放在 `hermes-volc-standalone/.hermes-settings.json`，两者是不同运行实例，各自在自己的后台修改。独立 Worker 版使用 `DB` 绑定和同一份迁移，未来部署前需将 `wrangler.toml` 的数据库占位 ID 替换为目标数据库并配置管理密码；本次未部署。

- 主应用：使用 Node.js 22.13 或更高版本。进入 `hermes-voice-coach`，复制 `.env.example` 为 `.env.local` 并填写服务凭据和管理密码，依次运行 `npm ci`、`npm run db:migrate`、`npm run dev`。打开 `http://localhost:3000/`。`npm test` 执行构建和测试，`npm run lint` 检查代码。
- 独立 Node 版：进入 `hermes-volc-standalone`，复制 `.env.example` 为 `.env.local` 并填写相同字段，运行 `node server.mjs`。不需要安装 npm 依赖。
- 独立 Worker 版：本地开发时将填写后的 `.env.local` 复制为 `.dev.vars`；云端凭据通过 `wrangler secret put` 配置。`wrangler.toml` 只包含运行与数据库绑定设置，不包含服务密钥。部署前仍需配置目标 D1 数据库。
- 两端需要 HTTPS 或 localhost；主应用与独立版若运行在不同端口，浏览器存储彼此独立。
- `.env.local`、`.dev.vars`、管理设置、本地数据库、日志、依赖和构建产物不提交。仓库内只提供空白凭据样例。独立版随附的 RTC SDK 许可见 [public/sdk/LICENSE](./hermes-volc-standalone/public/sdk/LICENSE)。

## 官方接口依据

- [声音登记](https://docs.volcengine.com/docs/6348/1804905)：录音条件和注册 API 版本 `2024-12-01`。
- [多人声纹](https://docs.volcengine.com/docs/6348/2122016)：Mode 2、最多 3 个预注册声纹、未知身份与设备条件。
- [开启对话](https://docs.volcengine.com/docs/6348/2123348)：沿用项目的 `2025-06-01` 接口与豆包 ASR / LLM / TTS。
- [判停与触发](https://docs.volcengine.com/docs/6348/1544164)：使用 `TurnDetectionMode=0` 自动结束识别；手动模式必须发送结束指令，不能等待自动结束标记。
- [更新对话](https://docs.volcengine.com/docs/6348/2123350)：`UpdateParameters`、`ExternalTextToLLM` 和 `interrupt`。
- [上下文](https://docs.volcengine.com/docs/6348/1511926)：SystemMessages、TopUserPrompts 和自动历史。
- [实时字幕](https://docs.volcengine.com/docs/6348/1337284)：客户端 ASR 累积文本、AI 增量文本和身份字段。
