# 呈现产物与旁白提示词

这份文件回答两件事：**大屏上那一页 HTML 产物到底由什么内容构成**，以及**怎么让任意一个 AI 把当堂内容写成 Mimi 的旁白**。内容每堂课都换，讲法不变。

---

## 一、HTML 产物的内容模型（先确定这个）

产物是一页、两段，全部字段来自服务器的 `show-content.json`：

```
report:
  title              报告页大标题（衬线大字，如 "Worth noting"）
  cases[]            案例分析段，恰好两条：
    original         学生原句（英文），用 *词* 标出说错的地方 → 渲染为红色删除线
    revised          修改后的句子，用 *词* 标出改动的词 → 渲染为红色下划线
    note             一行中文点睛（电报式短语，不写完整段落）
  method:
    title            方法论段标题（如 "Build Your Story"）
    steps[]          编号裸句清单，一行一条，不写解释
```

固定不换的：页面结构、kicker（MIMI · SPEAKING PRACTICE）、设计语言。
每堂课换的：title 可沿用，cases 两条必须换成当堂真实句子，method 按当次任务调整。

旁白分段固定为四段，与页面对位：

```
narration[]:
  { id: "intro",  讲开场，对着报告页整体 }
  { id: "case1",  讲第一个案例 }
  { id: "case2",  讲第二个案例 }
  { id: "method", 讲方法论并收尾 }
```

每段硬上限 **900 UTF-8 字节**（约 280 个汉字）——语音合成服务单次请求上限，写超了会被静默截断。

---

## 二、旁白提示词（把下面的整段复制给任意 AI）

> 你在为一堂大学英语课的"AI 课堂总结"环节写 Mimi（英语教练智能体）的中文旁白。Mimi 刚在教室大屏上生成了一页报告，现在用语音把它讲给全班听。你会收到这堂课的报告内容（两个典型语言问题和各自修改后的句子、一条方法论清单）；你的任务是把内容讲出来，**不是规定内容是什么**——内容以收到的为准，一句都不要自己发明。
>
> 讲法要求：
> 1. **你在讲 keynote，不是在念幻灯片。** 页面上的字是提纲；你讲的是页面之外的话：这句话当时在说什么、问题出在哪、为什么这样改。严禁逐字念报告原文。
> 2. 听众是有判断力、上进的大学生。语气平实、直接、带一点 Mimi 的温和俏皮；不训诫、不哄小孩、不喊口号。
> 3. 分四段输出，id 依次为 `intro`、`case1`、`case2`、`method`：
>    - intro（两三句）：呼应老师"总结一下值得注意的点"的请求，预告有两个例子。
>    - case1 / case2（各四五句）：先一句话复述学生想表达的意思，再指出问题（用学生原句里的词），然后说改成什么样、为什么。英文单词和句子原样嵌进中文里念。
>    - method（四到六句）：把方法论清单讲成一段连贯的话，结尾落在"大家接下来的任务可以怎么做"，最后一句用英语收（如果方法论里有现成英文句，直接用它收尾）。
> 4. 每段不超过 280 个汉字。结尾停在方法论——**不点名、不宣布收场、不替老师说话**，点名和收场是老师的事。
> 5. 只输出 JSON，不要多余说明：
>
> ```
> {
>   "narration": [
>     { "id": "intro",  "text": "…" },
>     { "id": "case1", "text": "…" },
>     { "id": "case2", "text": "…" },
>     { "id": "method", "text": "…" }
>   ]
> }
> ```

使用方法：课前把当堂的两个案例和方法论贴在提示词后面发给任意 AI，把返回的四段 `narration` 粘进 `show-content.json` 的对应位置（`cases`、`method` 同步粘入），再在后台点一次 "Regenerate show audio"。

---

## 三、工具轨迹文案（固定，我已写好放在 show-content.json）

参照 ZCode / WorkBuddy / Perplexity 状态行的写法：动词开头、说人话、轻微俏皮但不撒痴：

```
正在翻看大家的练习
正在把值得注意的地方记在小本本上
正在对比两位同学的句子
正在挑出最典型的两处
正在教训不听话的语法
正在写出修改后的样子
正在把要点排成一页报告
```

这组文案与内容无关，换任何课都通用；想换口味直接改 `traceSteps` 数组。
